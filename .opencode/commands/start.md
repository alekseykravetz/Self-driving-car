---
description: Start the full dev environment (tsc watch + serve :9090 + lint/format watcher)
agent: build
---

Run `npm start` from the project root. This runs three concurrent watchers in the same terminal.
